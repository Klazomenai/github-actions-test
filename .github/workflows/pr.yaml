name: "Integration Tests"

on:
  pull_request:
    branches:
      - master
      - foo
      - bar
      - biscuit

jobs:
  integration_tests:

    if: github.ref ==  'foo' || github.ref == 'bar' || github.ref == 'biscuit'

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      name: git checkout

    - name: Sed branch out of ref
      run: echo $GITHUB_HEAD_REF $GITHUB_RUN_NUMBER
